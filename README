# AI Voice Assistant

Голосовой ассистент на Python с распознаванием речи через Vosk, интерфейсом на PyQt6 и интеграцией с DeepSeek API.
Поддерживает как **текстовый**, так и **голосовой ввод**, озвучивает ответы через `pyttsx3`.

---

🚀 Функциональность

- 🎤 Голосовой ввод через Vosk + PyAudio
- 🧾 Текстовый ввод через поле ввода (QLineEdit)
- 🤖 Обработка команд через DeepSeek API
- 🔊 Озвучивание ответов через pyttsx3
- 🖥 GUI через PyQt6
- 💬 История диалога** в окне чата


---

## ⚙️ Установка

1. Клонируйте проект:

```bash
git clone <repository_url>
cd Ai_voicehelp

Установите зависимости:
pip install -r requirements.txt

Скачайте модель Vosk и положите в папку model/
https://alphacephei.com/vosk/models - vosk-model-small-ru-0.22

Создайте .env в корне проекта:
DEEPSEEK_API_KEY=ваш_ключ_DeepSeek

Сервер (FastAPI)
Пока сервер нужен только для структуры, можно запускать, если используете отдельные эндпоинты.
uvicorn server.main:app --reload

2️⃣ Клиент (GUI)
python -m client.main
Важно: запуск через -m гарантирует корректное определение путей к пакетам.

🧩 Как пользоваться
Текстовый ввод: введите фразу в поле ввода → нажмите Enter или кнопку «Отправить»
Голосовой ввод: нажмите кнопку «🎤 Говорить» → произнесите фразу → ассистент ответит текстом и озвучкой
История диалога отображается в QTextEdit сверху

🛠 Технические детали
Vosk — распознавание речи
PyQt6 — GUI и многопоточность (QThread для голоса)
pyttsx3 — синтез речи (TTS)
DeepSeek API — генерация ответов
requests + dotenv — подключение к DeepSeek через .env

Структура потоков:
🎤 Голос → QThread → MainWindow.process_text()
⌨️ Текст → MainWindow.process_text()
MainWindow.process_text() → DeepSeek → ответ → speak()